# CLAUDE_MRA_2

## Harness: Medical Literature Review

**Goal:** Produce high-quality, in-depth, systematic-style medical literature reviews from live,
up-to-date sources (PubMed/PMC, Elicit, bioRxiv/medRxiv, ClinicalTrials.gov, Consensus), with a constitution,
a human-approved Research Map gate, provenance-on-disk, rubric+audit scoring, and a learning loop.

**Constitution:** `.claude/constitution.md` — the 6 immutable Laws bind every agent. Law 1 (no
fabricated citations → auto-fail) and the Research Map hard gate are non-negotiable.

**Trigger:** For any request to write, update, expand, redo, score, or audit a medical/clinical/
biomedical literature review (or "review the evidence on <topic>"), use the
`medical-review-orchestrator` skill. It runs a 7-agent team (strategist → retriever → appraiser →
writer → coach → verifier, plus a lessons-curator). A single factual medical question is answered directly.

**Four human gates:** (1) Phase-0 scope confirm; (2) Gate 2b post-retrieval (corpus + full-text/source
gaps, L-022/L-024); (3) Phase-3 **Research Map** — the **hard gate**; (4) Gate 4b post-appraisal (GRADE +
contradictions + assumptions, L-024). All four run on every effort level (incl. `tiny`); the Research Map
hard gate has no small-scope exception. The team STOPS and waits for explicit user approval at each.

**Directory contract:**
- `reference/<topic>.md` — persistent verified-citation store; the writer cites ONLY from here.
- `source/<folder>/` — user-supplied full-text PDFs; checked and asked-about every run.
- `_workspace/` — per-run artifacts (preserved for audit / partial re-runs). **`NN` = the phase that
  produces it; `NN` with no letter = the artifact a human reads; `NNa…` = supporting or machine
  artifacts, lettered in production order.**

| Artifact | Phase | Who writes it | Read at |
|---|---|---|---|
| `00_scope.md` | 0 | lead | — |
| `01_protocol.md` · `01a_concepts.json` | 1 | research-strategist | — |
| `02_corpus.md` | 2 | evidence-retriever | Gate 2b |
| `02a_search_log.md` | 2 | evidence-retriever | Gate 2b |
| `02b_records.jsonl` · `02b_records_external.jsonl` · `02b_import.md` | 2 | retriever · `import_external.py` | — |
| `02c_studies.jsonl` → `02d_dedup.md` → `02e_candidates.jsonl` → `02f_deferred.jsonl` → `02g_worksheet.md` → `02h_verdicts.jsonl` → `02i_prisma.json` | 2 | P4 scripts + retriever | — |
| `02j_prisma.md` | 2 | `prisma_flow.py` | Gate 2b |
| `03_research_map.md` | 3 | lead | **hard gate** |
| `03a_gate_approval.md` | 3 | lead (L-019) | audit |
| `04_appraisal.md` | 4 | critical-appraiser | Gate 4b |
| `04a_numbers.md` | 4 | `extract_numbers.py` | — |
| `04b_cards.jsonl` | 4 | critical-appraiser | — |
| `04c_quote_locks.md` | 4 | `verify_quotes.py` | Gate 4b |
| `05_draft_review.md` | 5 | synthesis-writer | — |
| `05a_coach.md` | 5b | quality-coach | — |
| `06_final_review.md` | 6 | lead | **deliverable** |
| `06a_verification_report.md` | 6 | citation-verifier | delivery |
| `06b_citation_audit.md` | 6 | `citation_audit.py` | delivery |
| `06c_manifest.md` | 6 | lead | delivery |
| `07_proposed_lessons.md` | 7 | lessons-curator | user approval |

  **`02b` is one logical artifact in several files: the record stream and how it was built.**
  `02b_records.jsonl` is what the MCP sources returned; `02b_records_external.jsonl` is what the
  author's own database exports contributed; `02b_import.md` says which export files those came
  from, under which query, on which date. Concatenate both JSONL files before dedupe — the schema
  is identical, so nothing downstream distinguishes them except the `source` and `prisma_column`
  each record carries.

  **`source/<folder>/_exports/`** holds the author's bibliographic exports (RIS/NBIB/BibTeX/CSV)
  plus the `manifest.json` declaring each file's query, date, database and PRISMA column. The
  underscore keeps them apart from the full-text files in the same study folder.

  **Re-running a phase makes everything downstream stale.** The numbering makes this checkable: after
  re-running phase `N`, every artifact numbered above `N` describes a run that no longer exists.
  Re-generate them or delete them — never leave a stale `06_final_review.md` next to a fresh
  `04_appraisal.md`, and never let the manifest index artifacts from two different runs.

  **Reading an old evolution-log entry.** Entries before 2026-09-05 use the pre-renumbering names, and
  the log is a historical record that is never rewritten. Translate with:
  `00_protocol`→`01_protocol` · `01_search_log`→`02a_search_log` · `03_appraisal`→`04_appraisal` ·
  `03b_numbers`→`04a_numbers` · `04_draft_review`→`05_draft_review` · `04b_coach`→`05a_coach` ·
  `05_verification_report`→`06a_verification_report` · `05b_citation_audit`→`06b_citation_audit` ·
  `08_manifest`→`06c_manifest` · `research_map_gate_approval`→`03a_gate_approval`.
  `02_corpus`, `06_final_review` and `07_proposed_lessons` are unchanged.

  **Exports are not in the contract yet.** One completed run wrote a hand-built `_workspace/index.html`
  (a styled HTML rendering of its final review). Producing an export is legitimate and useful, but it
  has no agreed name or generator — **P6 will replace it with a proper CSL/Pandoc path** (`.docx` to the
  target journal's style, plus a publish gate that refuses to export while the citation audit is
  failing). Until then, treat any export as ad-hoc: `06_final_review.md` remains the deliverable of
  record, and nothing downstream may depend on an export existing.
- `.claude/skills/lessons-learned/lessons.md` — distilled digest (injected each run) ·
  `evolution-log.md` — full archive (not auto-loaded).
- `.claude/tests/` — automated regression suite; run
  `python3 -m unittest discover -s .claude/tests -t .claude/tests` after ANY harness edit.

**Config defaults:** systematic rigor (PRISMA + GRADE) · Vancouver citations · **output language
defaults to Vietnamese but is user-selectable per review** (confirm in Phase 0) · lessons saved only
after user approval.

**Workflow — branch-per-study (repo hygiene):** The harness on `main` is the reusable tool; each
research project is its own data. For every new review: (1) `git checkout main && git pull && git
checkout -b review/<topic>` so the branch inherits the latest harness + lessons; (2) run the harness on
that branch — `source/`, `reference/<topic>.md`, and `reviews/<topic>/` land here and are committed to
the branch; (3) when done and the user has approved the lessons, update `main` with a *small* commit
touching ONLY the three cross-session knowledge files — `lessons.md`, `evolution-log.md`, and
`vi-terminology.md` (the glossary is part of "the lesson"; omitting it lets terminology errors recur);
(4) keep `review/<topic>` as the permanent archive of that study — do NOT merge it into `main`. Always
branch from a fresh `main` so each study starts with the newest lessons.

**Archive branches (do NOT merge into `main`):** every completed study lives on its own branch, each
carrying only that study's `reference/`, `source/`, `reviews/` and `_workspace*` artifacts. `main`
keeps the harness plus empty `.gitkeep` scaffolding for the four data directories.

| Branch | Study | evolution-log |
|---|---|---|
| `review/af-ablation-metrics` | Ablation metrics in RF-PVI for AF (AI/LSI/LID/CF/AID) — final review is in `_workspace/06_final_review.md`, it has no `reviews/` folder | #4 |
| `review/cryoballoon-pfa-af` | Cryoballoon in the PFA era · CBA vs RF (+PFA) in the elderly | #7, #8 |
| `review/la-ep-elderly-af` | Left-atrial electrophysiology in elderly AF | #5, #6, #10 |
| `review/test-cases` | Regression baselines — metformin (EASY) + semaglutide/GLP-1 (HARD) | #1, #2, #3 |

To revisit a study: `git checkout review/<topic>`. To *continue* one, branch a fresh
`review/<topic>-v2` from current `main` and copy in the store — never build new work on an archive,
whose harness is frozen at the version the study was run with. (Note: branching keeps *future*
checkouts light but does not shrink `.git` history; that is acceptable on a private repo.)

**Change log:**
| Date | Change | Target | Reason |
|------|--------|--------|--------|
| 2026-06-13 | Initial build — 6 agents, 7 skills, orchestrator, seeded lessons store | full harness | - |
| 2026-06-13 | Ported v1 (CLAUDE_MRA) disciplines: constitution (6 Laws), Research Map hard gate, provenance store + `source/` folder, rubric + audit, two-tier learning (evolution-log), curiosity budget + assumption register, Vietnamese default + verified terminology glossary | constitution, orchestrator, all agents/skills, new references | Learn from battle-tested v1; keep v2's multi-agent + GRADE/PRISMA + live trial-registry edge |
| 2026-06-14 | Live test review run to the Research Map gate (GLP-1 case) — validated retrieval, error-retry, provenance store, and the gate | _workspace/, reference/ | Verify the harness end-to-end |
| 2026-06-14 | Added lightweight regression layer: 3 fixed test cases + qualitative ratchet (per-criterion + law/gate, no brittle 0.01 numeric gate) | orchestrator references/test-cases.md | Guard against quality regression as the harness evolves, without v1's costly AutoTest |
| 2026-06-14 | First full review completed (semaglutide CV prevention, MET 0.82); learning loop closed — 5 lessons (L-008…L-012) + evolution-log Entry #1 + 6 glossary terms approved and saved; run recorded as Test Case 2 baseline | lessons.md, evolution-log.md, vi-terminology.md | Validate end-to-end incl. the human-approved learning loop |
| 2026-06-14 | Test Case 1 (metformin EASY) run; L-013 + evolution-log Entry #2 saved | lessons.md, evolution-log.md | Seed the EASY baseline; validate the ratchet |
| 2026-06-14 | **Correction (user-caught):** TC1 self-cleared the Research Map gate and assumed Phase-0 scope; QA falsely recorded "gate cleared." Corrected records (Entry #2, TC1 audit → process FAIL, score 0.84→0.81, baseline PROVISIONAL); hardened Phase 0 + Phase 3 + audit (gate-cleared now requires a quotable user approval; fail closed); added L-014 (gate no exception) + L-015 (always ask depth/purpose before writing) | orchestrator SKILL.md, audit.md, lessons.md, evolution-log.md, TC1 QA | Honor the two human gates; stop the audit from laundering gate breaches |
| 2026-06-14 | Properly-gated metformin re-run (Entry #3): Phase-0 confirmed (user changed depth 1500→500w), Research Map approved via quotable "approve" before drafting; MET 0.81 | evolution-log.md | Positive evidence the gate corrections hold |
| 2026-06-23 | Harness fix + PR salvage: added missing `quality-coach` agent (was spawned but undefined → would break TeamCreate); unified `vi-terminology` to one canonical file; replaced blanket all-opus spawn rule with per-agent frontmatter allocation; fixed dead `(L-023)`→`L-022` pointer. Repo set **PRIVATE** (resolves full-text copyright exposure; no history rewrite needed). Salvaged two completed reviews into `reviews/<topic>/` (LA-EP elderly AF ← PR#2; cryoballoon-vs-PFA ← PR#3) plus their `reference/` stores & `source/` corpora; pulled PR#3 harness upgrades (anti-hedging + steelman in constitution; steelman/calibration in rubric; coach+manifest checks in audit). Closed PRs #1 (superseded), #2, #3. | CLAUDE.md, constitution.md, orchestrator, quality-coach.md, rubric.md, audit.md, reviews/, reference/, source/ | Make the harness runnable as documented; preserve finished reviews durably; keep gate + fix integrity |
| 2026-06-23 | Completed the PR#3 steelman/anti-hedging theme across the pipeline: added a **Steelman the opposing case** principle to `critical-appraiser` and a both-directions language-calibration + **Steelman before you conclude** bullet to `synthesis-writer` (the appraiser/writer halves of the constitution+rubric+audit changes already merged). Documented the **branch-per-study** workflow (study data on `review/<topic>` branches; only `lessons.md`+`evolution-log.md`+`vi-terminology.md` flow back to `main`). | critical-appraiser.md, synthesis-writer.md, CLAUDE.md | Make steelman/calibration consistent end-to-end; codify repo-hygiene workflow |
| 2026-06-29 | **P1 — Deterministic citation audit** (borrowed aglr-med's tactic, not its shallowness): added `scripts/citation_audit.py` (Python stdlib, zero-dep, no LLM/network, identical output run-to-run) as a mandatory traceability floor that runs AFTER the LLM citation-verifier and cannot be talked past — HARD-FAIL on `fabricated_citation`/`missing_in_store`/`placeholder_leftover`/`coverage_below_threshold` (exit 1, Law 1 not-deliverable); WARN on `number_not_in_source`/`uncited_claim`. Format-agnostic (keys on PMID/DOI/NCT; handles both `[REF-NNN]` and table-style stores; Vietnamese decimal commas). Regression: both completed reviews PASS deterministically, 0 false HARD-FAILs; FAIL-path fixtures included. Wired into citation-verifier agent + SKILL.md + audit.md. Branch `claude/mra-deterministic-layer-n0t4yj`. | citation-verifier.md, citation-verification/SKILL.md, .../references/audit.md, .../scripts/, CLAUDE.md | Close the gate-laundering hole: pull mechanical citation checks out of the LLM's hands into a layer that can't self-persuade |
| 2026-06-29 | **P2 — Deterministic number extraction for appraisal**: added `evidence-appraisal/scripts/extract_numbers.py` (Python stdlib, zero-dep, no LLM/network) that pulls VERBATIM numbers from `reference/<topic>.md` into five typed buckets per record — `sample_sizes·percentages·p_values·confidence_intervals·ratios` (OR/RR/HR/aHR/MD/SMD/β/coef). The appraiser now LOADS evidence-table numbers from `_workspace/04a_numbers.md` instead of hand-copying (kills the broken-decimal / Methods-as-result Law-1 hazard); all interpretation (which number is the graded result, RoB, GRADE, mean±SD) stays with the LLM. Metadata (PMID/DOI/URL/date) scrubbed as spans so a findings line ending in "(Source: …DOI…)" keeps its numbers; word-boundary guards stop "Circ" matching "CI". Deterministic on both stores (identical reruns); empty bucket = "(none)", never a placeholder. Wired into evidence-appraisal SKILL §4 + critical-appraiser agent. | evidence-appraisal/SKILL.md, critical-appraiser.md, evidence-appraisal/scripts/, CLAUDE.md | Take the mechanical number-copying out of the LLM's hands; keep the depth of judgment with it |
| 2026-06-29 | **P3 — Recall proof + reproducibility for search**: codified three retriever habits — (1) cheap **count-target probe first** (`esearch retmax=0` / `countTotal=true`) before pulling, (2) **paginate to `retrieved==total`** or cap with a logged reason (a one-page MCP pull must not pose as complete), (3) log the **reproducible call** (params/URL), not just the human query — emitted as a fixed **"Recall & reproducibility ledger"** table. Added `literature-retrieval/scripts/validate_search_log.py` (Python stdlib, zero-dep, no LLM/network, offline) that HARD-FAILs (exit 1) on a missing ledger, missing column, non-integer count, a `call` with no params/URL, or a recall verdict inconsistent with the numbers. Format-check only (completeness/self-consistency, NOT whether counts are true or sources well-chosen); multi-source breadth unchanged. Valid/invalid fixtures included; deterministic (identical reruns). Wired into literature-retrieval SKILL + evidence-retriever agent. | literature-retrieval/SKILL.md, evidence-retriever.md, literature-retrieval/scripts/, CLAUDE.md | Make recall claims auditable — pull the "did you get everything / can it be rebuilt" check out of the LLM's narration into a layer that can't be implied past |
| 2026-09-05 | **Elicit wired in + repo hygiene**: added Elicit to the source map (`search_papers`/`search_trials` — semantic *or* Lucene-keyword search over a corpus wider than PubMed, up to 10k hits, filters PubMed cannot express: `typeTags`, `maxQuartile`, `hasPdf`, `retracted`). Three disciplines attached: (1) **retraction screening** — `retracted` is the only such check in the whole source map, and a retracted study cites cleanly (real PMID, real record) so the verifier cannot catch it → keep `exclude_retracted` on every search, re-check key studies with `only_retracted` before the Research Map gate, BLOCK on a hit, log the check even when empty (R4); (2) **credit gate** — `create_systematic_review`/`create_report` spend the user's money, so they are presented with parameters + `get_usage` at Gate 2b and need a quotable OK, fail closed (L-022/L-024), and run at `depth: thorough` because `fast` returns decisions with no supporting quotes (R4); (3) **retrieval ≠ appraisal** — Elicit's screening/extraction enters `reference/` as ordinary records; GRADE and RoB are never inherited from it. Also removed a stray 18 MB `26260733_Reddy_TOCCASTAR_2015.html` from the repo root (byte-identical to the `source/af-ablation-metrics/` copy modulo CRLF; verified before deleting). | literature-retrieval/SKILL.md, evidence-retriever.md, orchestrator SKILL.md, CLAUDE.md, repo root | Close the largest remaining capability gap against available tooling, and plug a Law-1 hole (retracted citations) the harness had no check for |
| 2026-09-05 | **Repo hygiene — `main` brought back into line with its own branch-per-study rule**: `main` was carrying every study's data (121 MB in `source/`, 5 reference stores, 2 `reviews/` folders, 3 `_workspace*` dirs) despite CLAUDE.md requiring study data to live on `review/<topic>` branches — and those branches did not exist. Created and pushed four archives from `main` — `review/af-ablation-metrics` · `review/cryoballoon-pfa-af` · `review/la-ep-elderly-af` · `review/test-cases` — each holding exactly one study, each with an `ARCHIVE.md` stating what it is and that it must not be merged. Verified lossless before stripping `main`: 84 data files and 82 blobs on `main`, 84 and 82 across the union of the archives, zero difference in either direction (the check was first run with a `grep` that silently dropped 9 filenames containing em-dashes — redone with `core.quotepath=false`). `main` now holds the harness plus empty `.gitkeep` scaffolding: 40 tracked files, 460 KB working tree. History is untouched, so nothing is unrecoverable. | `main` tree, four new `review/*` branches, CLAUDE.md | Make the documented workflow true of the repo; keep future checkouts light; give each finished study a durable, self-describing home |
| 2026-09-05 | **P5 — automated regression suite** (Evisyn's tactic: a harness with no test suite cannot be edited safely). Added `.claude/tests/` — 58 stdlib-only tests, no network, no LLM, runnable as one command. Pins the deterministic layer's guarantees (every P1 HARD-FAIL category on its own trigger; the 40% coverage floor, asserted both from the source literal and end-to-end; P2's five buckets and priority order, Vietnamese decimal commas, metadata scrubbing, the `\bCI\b` guard; P3's seven ledger columns and each failure cause) plus the harness's own wiring — no dead `L-NNN` pointers in operational files, every agent the orchestrator spawns exists, every script a doc names exists, the deterministic layer imports stdlib only and never the network. The last four guard bugs this repo has actually shipped. **Validated by mutation:** 11 deliberate regressions were injected one at a time and all 11 turned the suite red. The first sweep exposed two defects in the tests themselves — a threshold test that rebuilt its own argparse instead of reading the script's, and a word-boundary test whose string did not discriminate — and a third in the loader: `importlib` served a stale `__pycache__` bytecode so the suite ran green against code that was not on disk (R5, rigged inspection). The loader now compiles from source text on every run. Also fixed one real dead pointer (`L-007` in the lessons-learned format example → `L-002`, which absorbed it on consolidation). | .claude/tests/, lessons-learned/SKILL.md, orchestrator SKILL.md, .gitignore, CLAUDE.md | Give harness edits a mechanical safety net, so the ratchet is enforced by a machine instead of by memory |
| 2026-09-05 | **P4 — deterministic screening + PRISMA** (the largest remaining gap: the harness had P1/P2/P3 over citations, numbers and the search log, but still let the LLM *narrate* the PRISMA flow). Three stdlib scripts under `literature-retrieval/scripts/`: `dedupe_records.py` (one row per STUDY — DOI/PMID/NCT then fuzzy title within a ±1-year guard; a same-title pair outside the guard is REPORTED, never merged; a merged study takes its identity from the published DOI, not the `10.1101` preprint one), `prefilter_records.py` (concept-coverage ranking + two recall floors; the score only ADDS and the scope bonus is worth strictly less than one concept hit; an empty scope signal means UNKNOWN, not OUT; the cost ceiling caps the percentage rule only — a floor may exceed it and says `over_cap` rather than trimming), and `prisma_flow.py` (draws only boxes for steps that ran; a **deferred** study is unread not excluded, a **retracted** one is its own removal, no full-text box without a full-text assessment; exit 1 when the stages contradict). Requires a new machine-readable `_workspace/02b_records.jsonl` written at retrieval time — `reference/<topic>.md` holds only what survived screening, so the identification and exclusion counts cannot be derived from it at all. 32 new tests; **13 mutations injected, 13 caught.** The tests found four real bugs during the build: the cost ceiling was overriding the recall floor, the scope bonus equalled a concept hit (so scope could outrank topicality), the sort had no final tiebreak (so input order changed which studies got screened), and an exclusion with an empty reason slipped past the PRISMA reason check. | literature-retrieval/{SKILL.md,scripts/}, evidence-retriever.md, orchestrator SKILL.md, .claude/tests/, CLAUDE.md | Take the PRISMA flow out of the LLM's narration and make it a count of records on disk |
| 2026-09-05 | **`_workspace/` renumbered to one rule + four orphan artifacts given owners.** The numbering had drifted out of step with the phases (`00_protocol` was phase 1, `03_appraisal` phase 4, `05_verification`/`06_final`/`08_manifest` all phase 6) and P4 had just pushed phase 2 to ten files, with the suffix `b` meaning "sub-artifact" in one place and "next step" in another. New rule: **`NN` = the phase that produces it · `NN` = what a human reads · `NNa…` = supporting/machine artifacts in production order**. The three most-referenced artifacts (`02_corpus.md`, `06_final_review.md`, `07_proposed_lessons.md`) keep their names, so churn fell on the rarely-named files; 20 files rewritten, `evolution-log.md` deliberately NOT rewritten (it records what past runs actually wrote) with an old→new mapping added to the contract instead. Four fixes alongside: (1) **`01a_concepts.json` had no author** — P4's prefilter requires it and nothing produced it, a real gap introduced with P4; now owned by the research-strategist, with the warning that MeSH descriptors are useless here because the prefilter cannot expand them; (2) **Phase 0 now persists `00_scope.md`** — it previously left no artifact at all, so "the user confirmed the scope" was a claim with no receipt (R4); (3) **the Research Map is written to `03_research_map.md` before being presented** — previously only the approval was persisted, leaving the audit nothing to check the approval against; (4) staleness rule + export status documented. | `_workspace/` contract in CLAUDE.md, orchestrator SKILL.md, research-strategist.md, 17 other harness files | Make the number mean the phase, so the layout stays legible as steps are added — and close the orphan-artifact gap P4 opened |
| 2026-09-05 | **P8 — verbatim quote & number locks** (`citation-verification/scripts/verify_quotes.py`). P1 proves a citation is *traceable*; its own docstring concedes "it cannot prove it is TRUE." P8 narrows that by one honest step: it proves the QUOTE IS REALLY IN THE SOURCE FILE ON DISK, character for character, and that the numbers in the claim are numbers **that quote** contains — not merely numbers somewhere in the record. Five HARD locks, each rejecting a card (and so its claim): **IDENTITY** (the file carries this study's own identifier — a real quote attached to the wrong paper is the failure the other four structurally cannot see) · **QUOTE** (literal substring, no fuzzy matching) · **NUMBER** (every number in the claim is in its own quote) · **LENGTH** (40–1500 chars: too short proves nothing specific, a whole page contains the number by accident) · **FULLTEXT** (a card tagged `fulltext` whose quote lies inside the abstract is claiming depth it did not read — R4). Decimals are canonicalised so a Vietnamese `42,9%` matches an English `42.9%`, without blurring `0,48` into `0,84`; embedded `<script>`/`<style>` bodies are excluded so publisher JSON-LD cannot be quoted as the authors' prose. **A sixth lock was deliberately NOT ported**: the lineage's ECHO fingerprints an untrusted transcript against the abstract, but here the source file is on disk, so IDENTITY+QUOTE check the same thing directly — porting it would add a lock that cannot fail. PDFs are refused with an actionable message rather than read, keeping the layer stdlib-only. 26 tests; **11 mutations injected, 11 caught** (the first sweep exposed a test whose string did not discriminate, so it passed with the guard deleted). Wired: appraiser builds `04b_cards.jsonl` and fixes rejections, Gate 4b shows the accepted/rejected count, and the citation-verifier re-runs the locks itself rather than inheriting the appraiser's verdict. | citation-verification/scripts/, critical-appraiser.md, citation-verifier.md, orchestrator SKILL.md, .claude/tests/, CLAUDE.md | Move the citation floor from "this record exists" to "this sentence's number is in a quote that is really in the paper" |
| 2026-09-05 | **P7 — recall widened past the MCP source map.** Three parts, in descending order of what they actually buy. (1) **`import_external.py`** — parses the author's own Scopus / Web of Science / Embase / CENTRAL exports (RIS · NBIB/MEDLINE · BibTeX · CSV) into the same record schema, so P4 dedupes and screens them with no special casing. The harness has no access to those platforms and **must never try**: an agent querying them uses someone's institutional subscription against its licence. It reads files from disk, nothing else. Three refusals: a file with **no manifest entry is not imported** (an export carries records but not the query, date, database or reported count that PRISMA-S requires — an unrecorded search would sit in the corpus looking recorded); **`prisma_column` is declared, never guessed** (a Scopus export arrived by hand but is still a *database search*, and filing it under "other methods" understates the systematic search — `prisma_flow.py` now honours the declaration over its own source-name guess); and **`n_reported` is compared against records parsed**, because most platforms cap one download far below the result count and a truncated export is the commonest silent recall loss in a manual workflow. (2) **Europe PMC · OpenAlex · Semantic Scholar** documented as no-key `WebFetch` calls with the exact URL recorded in the P3 ledger. (3) **Pacing written as a discipline, not a mechanism** — nothing here throttles anything, and saying otherwise would claim a safeguard that does not exist. 26 new tests; **14 mutations injected, 14 caught.** The sweep exposed a real parser bug the first tests were too weak to see: **RIS continuation lines were dropped**, truncating every wrapped abstract at its first physical line — abstracts feed concept matching and screening, so that lost recall in silence. Also a fourth instance of the suite's recurring trap: a required-fields test that iterated the very tuple it was asserting. | literature-retrieval/{SKILL.md,scripts/}, prisma_flow.py, .claude/tests/, CLAUDE.md | Reach the literature behind the author's subscription without ever using their credentials — and make a manual export as auditable as an API pull |
